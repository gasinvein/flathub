app-id: com.vivaldi.Vivaldi
runtime: org.freedesktop.Platform
runtime-version: '20.08'
sdk: org.freedesktop.Sdk
command: vivaldi
finish-args:
  - --share=ipc
  - --socket=x11
  - --device=dri
  - --socket=pulseaudio
  - --share=network
  - --talk-name=org.freedesktop.Notifications
  - --talk-name=org.freedesktop.FileManager1
  - --talk-name=org.freedesktop.secrets
  - --talk-name=org.gnome.SessionManager
  - --system-talk-name=org.freedesktop.Avahi
  - --system-talk-name=org.freedesktop.UPower
  - --system-talk-name=org.freedesktop.NetworkManager
modules:
  - shared-modules/libsecret/libsecret.json

  - name: zypak
    sources:
      - type: git
        url: https://github.com/refi64/zypak.git
        tag: v2021.02

  - name: vivaldi
    buildsystem: simple
    build-commands:
      - mkdir -p /app/{bin,lib}
      - cp /usr/bin/ar /app/bin
      - cp /usr/lib/$(gcc -print-multiarch)/libbfd-*.so /app/lib
      - install -Dm755 apply_extra.sh /app/bin/apply_extra
      - install -Dm755 vivaldi.sh /app/bin/vivaldi
      - install -Dm755 stub_sandbox.sh /app/bin/stub_sandbox
      - install -Dm644 -t /app/share/appdata/ $FLATPAK_ID.appdata.xml
    sources:
      - type: script
        dest-filename: apply_extra.sh
        commands:
          - export FLATPAK_ID=com.vivaldi.Vivaldi
          - ar p vivaldi.deb data.tar.xz | tar -xJf -
          - mv opt/vivaldi .
          - install -Dm644 usr/share/applications/vivaldi-stable.desktop export/share/applications/$FLATPAK_ID.desktop
          - for s in 16 22 24 32 48 64 128 256;
              do install -Dm644 vivaldi/product_logo_$s.png export/share/icons/hicolor/${s}x${s}/apps/$FLATPAK_ID.png;
            done
          - sed "s|Exec=/usr/bin/vivaldi-stable|Exec=vivaldi|g" -i export/share/applications/$FLATPAK_ID.desktop
          - sed "s|Icon=vivaldi|Icon=$FLATPAK_ID|g" -i export/share/applications/$FLATPAK_ID.desktop
          - rm -r etc usr opt vivaldi.deb
          - cp /app/bin/stub_sandbox vivaldi/vivaldi-sandbox

      - type: script
        dest-filename: vivaldi.sh
        commands:
          - exec zypak-wrapper.sh /app/extra/vivaldi/vivaldi "$@"

      - type: script
        dest-filename: stub_sandbox.sh
        commands:
          - 'echo "Stub sandbox ignoring command: $@"'
          - exit 1

      - type: file
        path: com.vivaldi.Vivaldi.appdata.xml

      - type: extra-data
        only-arches:
          - "x86_64"
        url: "https://downloads.vivaldi.com/stable/vivaldi-stable_2.2.1388.37-1_amd64.deb"
        filename: vivaldi.deb
        size: 58076356
        sha256: a5ec363b0d3e53a4a6119041f6ec19a6082d60dacd7a4ef39dc54acb3725001f
        x-checker-data: &x-checker-data
          type: debian-repo
          root: http://repo.vivaldi.com/stable/deb/
          dist: stable
          component: main
          package-name: vivaldi-stable

      - type: extra-data
        only-arches:
          - "i386"
        url: "https://downloads.vivaldi.com/stable/vivaldi-stable_2.2.1388.37-1_i386.deb"
        filename: vivaldi.deb
        size: 56996270
        sha256: 347f53a08df6370be0ad80ab9bde97caa7bf8c61ab7f6b3da6e72707b9035a34
        x-checker-data: *x-checker-data

      - type: extra-data
        only-arches:
          - "arm"
        url: "https://downloads.vivaldi.com/stable/vivaldi-stable_2.2.1388.37-1_armhf.deb"
        filename: vivaldi.deb
        size: 50918454
        sha256: 2fcef2b1868bc2ee701cfb76ac0c517292d66fad556fa453634cff3e6a96bde4
        x-checker-data: *x-checker-data
